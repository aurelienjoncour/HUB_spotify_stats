
/**
 * Get top artists spotify list of a user
 *
 * @param {string} timeRange time range for the information asked
 * @param {string} limit number of element return by the request
 * @returns {Promise} top artists ranking by spotify
 */
const getUserTopArtists = async (timeRange, limit) => {
    
}

/**
 * Get top tracks spotify list of a user
 *
 * @param {string} timeRange time range for the information asked
 * @param {string} limit number of element return by the request
 * @returns {Promise} top tracks ranking by spotify
 */
const getUserTopTracks = async (timeRange, limit) => {
    
}

export {
    getUserTopArtists,
    getUserTopTracks,
}